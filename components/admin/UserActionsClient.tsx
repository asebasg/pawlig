"use client"

import { Button } from "@/components/ui/button";
import BlockUserButton from "@/components/admin/BlockUserButton"

interface UserActionsClientProps {
    isLoading: boolean;
}

export function UserActionClient({
    isLoading
}: UserActionsClientProps) {
    return null;
}